package main

func main() {
	word1 := "amanda é bonita"
	word2 := "amanda é bonita"

	if word1 == word2 {
		println("as duas strings são iguais")
	} else {
		println("as duas strings são diferentes")
	}
}
